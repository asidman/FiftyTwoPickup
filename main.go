package main

func main() {

	cards := newDeck()

	cards.print()
	//card := newCard()
	//card := []string{newCard(), newCard()} creating a slice
	//card = append(cards,"Six of Spades") adds to splice

}
